# Authorize-form-using-ReactJS
Authorization Form
Hiding contact details behind a password form which only authorized or a given password can be shown to.
Using a React component to set up an authorization layer.
